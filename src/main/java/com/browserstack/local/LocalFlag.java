package com.browserstack.local;


public enum LocalFlag {
    UNDEFINED,
    VERBOSE,
    FOLDER_TESTING,
    FORCE,
    ONLY,
    FORCELOCAL,
    LOCAL_IDENTIFIER,
    ONLY_AUTOMATE,
    PROXY_HOST,
    PROXY_PORT,
    PROXY_USER,
    PROXY_PASS,
    FORCE_PROXY,
    HOSTS
}
